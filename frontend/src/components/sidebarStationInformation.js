import { Typography } from "@mui/material";

export default function SidebarStationInformation(){
    return(
        <div style={{ paddingLeft: '20px' }}>
            <Typography variant="h5">
                Sidebar Station Information
            </Typography>
        </div>
    );
}