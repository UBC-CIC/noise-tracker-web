import { Typography } from "@mui/material";

export default function AdminGeneral(){
    return(
        <div>
            <Typography variant='h4' style={{ paddingTop: '20px', paddingLeft: '20px', paddingBottom: '50px' }}>Welcome to the NoiseTracker Admin Dashboard!</Typography>
        </div>
    );
}