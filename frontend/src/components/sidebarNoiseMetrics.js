import { Typography } from "@mui/material";

export default function SidebarNoiseMetrics(){
    return(
        <div style={{ paddingLeft: '20px' }}>
            <Typography variant="h5">
                Sidebar Noise Metrics
            </Typography>
        </div>
    );
}